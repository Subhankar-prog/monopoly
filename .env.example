# Frontend (Vite) - used when building the React app
# Set this to your deployed server URL for production
VITE_SERVER_URL=http://localhost:3001

# Backend (Node.js) - used when running the game server
# Port for the Socket.IO server
PORT=3001
